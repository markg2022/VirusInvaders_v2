y=y+sin(global.timer*global.frequency)*global.amplitude;

// check for collisions with player bullets
//if (collision_rectangle(x, y, x + sprite_width, y + sprite_height, obj_projectile1, false, true)) {
    // reduce the enemy's health by 10
//    maxHp -= 25;
//}

// check if the enemy's health has reached 0
//if (maxHp <= 0) {
    // destroy the enemy object
//    instance_destroy();
//}